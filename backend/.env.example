# ==============================================
# Kopi-O Backend Configuration
# ==============================================
# Copy this file to .env and update the values

# Security Keys (REQUIRED - Change in Production!)
# Generate strong random keys for production
SECRET_KEY=your-secret-key-change-in-production-use-random-string
JWT_SECRET_KEY=your-jwt-secret-key-change-in-production-use-random-string

# Flask Configuration
FLASK_ENV=development  # Options: development, production
FLASK_DEBUG=False      # Set to True for development only, MUST be False in production
PORT=5000              # Port for Flask server

# CORS Configuration
# Comma-separated list of allowed origins
# For production, specify exact domains instead of wildcards
CORS_ORIGINS=http://localhost:3000,http://localhost:5000,http://127.0.0.1:3000,http://127.0.0.1:5000

# Database
# SQLite database file location (default: kopio.db in backend directory)
# For production, consider using PostgreSQL or MySQL
# DATABASE_PATH=/path/to/kopio.db

# Email Configuration (for sending verification emails)
# REQUIRED for production to enable email verification
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_DEFAULT_SENDER=noreply@kopio.com

# For Gmail: Use App Password, not your regular password
# Go to: Google Account > Security > 2-Step Verification > App passwords

# Frontend URL (for verification links)
FRONTEND_URL=http://localhost:3000

# Email Configuration (Future use)
# MMU_EMAIL_DOMAIN=@mmu.edu.my

# Game Settings (Optional - defaults in config.py)
# QUIZ_QUESTIONS_PER_GAME=5
# QUIZ_TIME_LIMIT=60
# QUIZ_POINTS_PER_CORRECT=10
# MEMORY_GAME_BASE_POINTS=50
# PUZZLE_GAME_BASE_POINTS=30
# SORTING_GAME_BASE_POINTS=20

# JWT Settings
# JWT_ACCESS_TOKEN_EXPIRES=24  # Hours

# Logging
# LOG_LEVEL=INFO  # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_FILE=kopio.log

# Production Deployment Notes:
# 1. Generate strong SECRET_KEY and JWT_SECRET_KEY using:
#    python -c "import secrets; print(secrets.token_hex(32))"
# 2. Set FLASK_DEBUG=False
# 3. Set FLASK_ENV=production
# 4. Configure proper CORS_ORIGINS (specific domains, no wildcards)
# 5. Use HTTPS in production
# 6. Consider using PostgreSQL or MySQL instead of SQLite
# 7. Set up proper backup strategy for database
# 8. Configure rate limiting at reverse proxy level

